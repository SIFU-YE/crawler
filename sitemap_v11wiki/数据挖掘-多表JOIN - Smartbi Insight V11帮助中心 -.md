


多表JOIN是基于连接字段和给定的连接方式，将多个数据集合并为一张新的数据表。
支持单个或多个字段为连接字段，连接方式包括左连接、右连接、内连接、全连接。
## 输入/输出
六个输入端口，用于接收数据集。  
---  
一个输出端口，用于输出连接后的结果。  
## 参数设置
配置表关联关系：
配置表关联关系界面如下：
多个表合并需要设置多个关联条件，关联条件的说明如下：
左表 | 用于关联通过输入端口连接的前置节点资源输出的数据集。  
---|---  
右表 | 用于关联通过输入端口连接的前置节点资源输出的数据集。  
用于设置JOIN的连接类型：
  * 左连接：返回选择的左右表字段中，左表的全部数据和右表中满足关联条件的数据。
  * 右连接：返回选择的左右表字段中，右表的全部数据和左表中满足关联条件的数据。
  * 内连接：返回选择的左右表字段中，左表中满足关联条件的数据和右表中满足关联条件的数据。
  * 全连接：返回选择的左右表字段的所有数据。

  
用于设置查询的字段条件，分别选择关联的左侧表字段和右侧表字段。通过加号按钮可以增加关联条件。  
多表JOIN节点最多可接收6个数据源，连接数据源时需要从左到右按顺序连接且中间不能跳过端口，比如不能只连接第一个和第六个端口。
## 示例
1、选择4个数据源并成功运行，将多表JOIN节点拖入并按顺序连接，点击 **关联设置** 按钮。
2、设置这4个表之间的关联关系如图：
3、4个表通过设置的关联关系连接起来，得到一张新的表。  
