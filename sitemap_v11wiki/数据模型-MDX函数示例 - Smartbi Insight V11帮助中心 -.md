
为了简化数据模型定制人员的工作，系统实现了常用日期计算的MDX模板，主要有：环比、同比、同期值、累计值、历年累计值、固定维度聚合。
在函数面板中以“常用度量”目录存储MDX模板。
MDX模板设置如下：
该时间计算度量数据效果如下：
MDX模板设置如下：
该时间计算度量数据效果如下：
### 同期值
计算上年同期值的MDX模板设置如下：
该时间计算度量数据效果如下：
### 累计值
本年累计值MDX模板设置如下：
该时间计算度量数据效果如下：
### 历年累计值
MDX模板设置如下：
该时间计算度量数据效果如下：
### 固定维度聚合
MDX模板设置如下：
该时间计算度量数据效果如下：
为了简化数据模型定制人员的工作，系统实现了常用日期计算的MDX模板，主要有：环比、同比、同期值、累计值、历年累计值、固定维度聚合。
在函数面板中以“常用度量”目录存储MDX模板。  
