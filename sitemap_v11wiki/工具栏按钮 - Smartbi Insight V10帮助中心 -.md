
多维分析工具栏列出了所有可对多维分析进行操作的按钮。创建多维分析、创建多维探索和浏览多维分析稍有不同。
创建多维分析工具栏：
创建多维探索工具栏：比创建多维分析多出高级设置按钮。
下面表格列出所有的工具栏按钮：
在各种状态之间进行切换，主要实现从后一个状态回退到前一个状态。  
---  
在各种状态之间进行切换，主要实现从前一个状态前进到后一个状态。  
即刷新的功能，重新读取多维报表数据。  
保存当前多维报表到默认路径。系统只允许用户在多维报表定制界面中对多维报表执行保存操作。  
保存当前多维报表到用户指定路径。  
添加到收藏夹 | 把当前多维报表分析另存到"我的收藏夹"。  
把表格中的数据以图形的方式来展现。  
是否显示或是隐藏查询面板，只有在浏览多维分析状态才有此按钮。  
设置在工作区中显示的内容和展现方式。其中，多维报表定制界面和多维报表浏览界面允许选择的内容有区别：  
设置多维探索的相关属性，主要包括：表属性、设置表头、设置表尾。  
以实现对定制好的多维报表进行深入设计。 主要可以实现：自定义成员、自定义命名集、参数属性、维度过滤器设置、告警设置、已选自定义成员优先级设置。  
对多维报表中的数据进行汇总设置。  
该功能实现以下功能：
  * 设置有参数的多维探索保存后再次被打开时的参数取值。
  * 设置在 参数切换 取值后是否自动刷新数据。
  * 设置当前多维探索各参数的布局。

  
取消隐藏行列 | 用于取消隐藏行列的操作  
查看当前多维报表的MDX表达式。  
把设计区中的内容以WORD、PDF、CSV、TXT、Excel格式导出。  
用于查看当前报表执行各个功能的操作时间记录、所耗的时间等信息。  
