#### 通过
# CalculationCurrentPass (MDX)
针对指定的查询上下文返回多维数据集的当前计算传递。
```
  
CalculationCurrentPass()  

```

**CalculationCurrentPass** 函数返回当前查询上下文的计算传递的从零开始的索引。 对于自动递归解析，此函数几乎没有实际用途。
CalculationPassValue (MDX) IIf (MDX) MDX 函数参考 (MDX)
1月27日 15时 - 1月27日 15时 


