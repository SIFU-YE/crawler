#### 通过
# MDX 脚本编写语句 (MDX)
在多维表达式 (MDX) 中，下列语句用于管理 MDX 脚本中的上下文、作用域和流控制。
计算子多维数据集，还可以确定子多维数据集中所包含维度的求解次序。  
---  
允许您有条件地从多次比较中返回特定值。  
强制在当前上下文中计算所指定的集。  
将所指定子多维数据集的单元值锁定为其当前值。  
如果条件为真，则执行语句。  
将指定 MDX 语句的作用域限制于指定的子多维数据集。  
MDX 语句参考 (MDX) MDX 数据定义语句 (MDX) MDX 数据操作语句 (MDX) MDX 脚本编写基础知识 (Analysis Services)
1月27日 15时 - 1月27日 15时 


