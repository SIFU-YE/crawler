#### 通过
# MDX 数据定义 - DROP CELL CALCULATION
删除指定的单元计算。
```
  
DROP [ SESSION ] CELL CALCULATION CURRENTCUBE | Cube_Name.CellCalc_Name  

```

_Cube_Name_ 提供多维数据集表达式名称的有效字符串表达式。
_CellCalc_Name_ 提供要删除的单元计算名称的有效字符串表达式。
CREATE CELL CALCULATION 语句 (MDX) MDX 数据定义语句 (MDX)
1月27日 15时 - 1月27日 15时 


