#### 通过
# MDX 数据操作语句 (MDX)
在多维表达式 (MDX) 中，数据操作语句用于检索和操作来自多维对象的数据。 下表列出了 MDX 中的数据操作语句。
运行一个存储过程，它在当前作用域或指定的多维数据集（可选）中返回空值。  
---  
从多维数据集中删除所有计算，并将多维数据集恢复为计算传递 0。  
检索用来在多维数据集中创建指定单元的行集。  
从指定多维数据集中检索数据。  
更新多维数据集中的指定叶单元或非叶单元的值，并且可以根据需要跨依赖的叶单元为指定非叶单元分配值。  
MDX 语句参考 (MDX) MDX 数据定义语句 (MDX) MDX 脚本编写语句 (MDX)
1月27日 15时 - 1月27日 15时 


