#### 通过
# Var (MDX)
使用非偏置总体公式 (除以 _n_) ，返回对集计算的数值表达式的样本方差。
```
  
Var(Set_Expression [ ,Numeric_Expression ] )  

```

_Set_Expression_ 返回集的有效多维表达式 (MDX)。
_Numeric_Expression_ 返回数字的有效数值表达式，通常为单元坐标的多维表达式 (MDX)。
**Var** 函数返回对指定集计算的指定数值表达式的无偏方差。
**Var** 函数使用无偏差总体公式，VarP 函数使用有偏差总体公式。
1月27日 15时 - 1月27日 15时 


