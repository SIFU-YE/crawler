
## 执行
执行是指直接在Excel中查看报表数据。
### 功能入口
点击工具栏的 **执行** 按钮或 **选择参数执行** 按钮：
  *   * 

### 操作按钮
执行 |  直接在Excel中查看报表数据，不允许设置参数值。 电子表格有如下表样： 点击 **执行** 按钮，效果如图：  
---|---  
选择参数执行 |  设置参数值后，在Excel中查看报表数据。 点击 **选择参数执行** 按钮，弹出”参数值设置“界面，设置参数值，如图： 设置参数值为产品类别“点心”，效果如图： 数据模型的参数设置可参考文章数据模型-参数应用  
##  重置
重置是指在执行之后，将报表重置回初始状态。
### 功能入口
点击工具栏的 **重置** 按钮或 **重置（保留图表样式设置）** 按钮：
### 操作按钮
重置 |  指直接将电子表格设置回初始状态。 点击执行按钮之后，电子表格如图： 点击 **重置** 按钮，效果如图：  
---|---  
重置（保留图表样式设置） |  指将电子表格切换回初始状态时，保留在执行状态下对图形的样式设置。 图形必须为Excel图形。 1、在电子表格中创建了如下报表，其中包含一个柱图图表： 预览效果如下： 2、点击 **执行** 按钮，效果如图： 3、在执行状态下，对柱图进行格式设置，如图： 4、点击 **重置（保留图表样式设置）** 按钮，切换回初始状态。 5、预览图表，如图：图表展示的样式保留了在执行状态下的设置。  
